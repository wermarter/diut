apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bathanghai-server.fullname" . }}-test-connection"
  labels:
    {{- include "bathanghai-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bathanghai-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
