apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-grafana-dashboard
  namespace: observability
  labels:
    grafana_dashboard: '1'
data:
  'redis-dashboard.json': |
    {{ $.Files.Get "src/11835_rev1.json" | fromJson | toJson }}
