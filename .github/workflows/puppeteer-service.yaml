name: puppeteer-service

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'libs/common/**'
      - 'libs/nestjs-infra/**'
      - 'libs/services/**'
      - 'apps/example-service/**'

jobs:
  j1:
    uses: ./.github/workflows/docker-publish.yaml
    with:
      dockerfile-path: ./apps/puppeteer-service/Dockerfile
      docker-image-name: diut-example-service
    secrets: inherit

  j2:
    needs: j1
    uses: ./.github/workflows/helm-appversion.yaml
    with:
      chart-path: ./apps/example-service/k8s/helm-chart/Chart.yaml
      identity: example-service
    secrets: inherit
