apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diut.fullname" . }}-test-connection"
  labels:
    {{- include "diut.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diut.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
