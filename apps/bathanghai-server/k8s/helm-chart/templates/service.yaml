apiVersion: v1
kind: Service
metadata:
  name: {{ include "bathanghai-server.fullname" . }}
  labels:
    {{- include "bathanghai-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bathanghai-server.selectorLabels" . | nindent 4 }}
