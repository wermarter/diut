apiVersion: v1
kind: Service
metadata:
  name: {{ include "diut.fullname" . }}
  labels:
    {{- include "diut.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diut.selectorLabels" . | nindent 4 }}
