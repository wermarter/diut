apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    ports:
      port8000:
        port: 8002
        targetPort: 8001
        expose: true
        protocol: TCP
